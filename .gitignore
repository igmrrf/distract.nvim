# Build & Rust artifacts
target/
engine/target/
*.rs.bk

# Test artifacts & generated screenshots
.tests/
tests/screenshots/
*.png
!assets/*.png
# A bundled pet's packed sheet is what the overlay backend draws from, so it has to
# be tracked. `!assets/*.png` only reaches files directly under `assets/`, so the
# three imported pets' sheets were being ignored -- they would have shipped as
# manifests pointing at art nobody who cloned the repo had.
!assets/*/*_sheet.png
engine_render_screenshot.png

# Temporary & scratch scripts
*.py
!tools/codex_pets/*.py
engine/test_window.rs

# Agent review & planning artifacts
code_review.md
design_plan.md

# Python caches
__pycache__/
*.py[cod]
*$py.class
.venv/

# Editor & IDE files
.vscode/
.idea/
*.swp
*.swo
*.swn
*.un~
*~

# macOS metadata
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Downloaded release binaries (see :help distract-overlay)
engine/bin/

# Local sprite-import test material (236 MB of unlicensed third-party artwork).
# Kept on disk, deliberately never committed; `imported/` regenerates from
# `sheets/` via tools/codex_pets/. See HANDOFF.md.
assets/codex_pets/
