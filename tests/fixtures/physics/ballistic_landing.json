{
  "description": "A ballistic arc that fires `on_land` when it reaches its floor. The only fixture that declares a transition: every other one clears them so the trajectory keeps describing the fixture, but on_land's whole subject is *when* the state changes, which is exactly what could drift between the engines. `ground_y` is in terminal cells, like every other manifest position.",
  "physics": { "target_vx": 1.0, "target_vy": -4.0, "gravity": 0.35, "ground_y": 28, "locomotion": "ballistic", "friction": 0.05, "wrap_mode": "none" },
  "transitions": { "on_land": "landed" },
  "spawn": { "x": 30, "y": 28, "flip_x": false },
  "cell": { "w": 10, "h": 20 },
  "bounds": { "columns": 120, "lines": 40 },
  "dt": 0.016666667,
  "steps": 180
}
